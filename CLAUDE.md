# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Qué es este proyecto

Panel de control operativo interno de RS Tax & Legal (Rodríguez Samith Tax & Legal Limitada), firma chilena de servicios legales y contables con sede en Viña del Mar; dueño: Cristian López Thienel. Áreas: corporativo, laboral, tributario. El sistema gestiona los ciclos mensuales de cumplimiento de la cartera: liquidaciones de sueldo (Previred), conciliación compras/ventas (SII + KAME) y declaración F29.

Estilo de trabajo: español de Chile, fechas DD-MM-AAAA, moneda CLP. Citar normativa chilena cuando aplique (CT, SII, DT, CMF). Directo, técnico, prosa por sobre bullets, sin disclaimers genéricos. Si el usuario se equivoca, contradecirlo con fundamento normativo.

## Equipo (tabla `usuarios`)

- Cristian López — clopez@rstaxlegal.cl — admin
- Felipe Rodriguez Samith — frodriguez@rstaxlegal.cl — admin
- Solange Quintana Pizarro — squintana@rstaxlegal.cl — operador
- Danilo Tapia — dtapia@rstaxlegal.cl — operador

Cualquier correo @rstaxlegal.cl NO registrado en la tabla `usuarios` queda fuera del sistema (el login lo valida antes de enviar el magic link).

## Cartera y reglas de negocio (estado al 11-06-2026)

Total: 137 clientes (133 activos, 4 inactivos). En cartera hay 83 clientes-matriz (personas o grupos familiares) categorizados A/OPEN, B/SEGUNDA, C/TERCERA, D/PUBA; cada cliente-matriz puede tener múltiples empresas (sociedades) con su propio RUT.

Flags por cliente en tabla `clientes`:

- `hace_liquidaciones` BOOLEAN: cliente en ciclo mensual Previred (67 activos)
- `hace_f29` BOOLEAN: cliente en ciclo mensual F29 (123 activos)
- `hace_contabilidad_completa` BOOLEAN
- `es_profesional_salud` BOOLEAN (null = pendiente de clasificar)
- `kame_cert_estado` TEXT 'ON'/'OFF' (default ON)
- `activo` BOOLEAN

Regla: si `hace_liquidaciones=TRUE` casi siempre `hace_f29=TRUE` también, pero hay una EXCEPCIÓN clave: asesoras del hogar Art. 110 CT (hacen Previred pero NO F29, porque no son empresas con actividad económica). El constraint estricto se eliminó por esta excepción. Además, existen empresas activas SIN F29 ni Previred y son válidas — solo cartera (ej.: Comercial Multimaxusa Burgos).

## Ciclos mensuales (3 procesos separados)

1. `ciclo_liquidaciones` (Previred): 67 clientes × mes.
2. `ciclo_conciliacion` (descargas SII C/V + revisión KAME; lo lleva Solange): 123 clientes × mes.
3. `ciclo_f29` (armado y presentación): 123 clientes × mes. Plazo día 20 del mes siguiente, UNIFORME — no se diferencia por Previred (decisión explícita).

Cambios de IVA recuperable a no recuperable (solo profesionales de salud): tabla `iva_salud_ejecucion`, frecuencia flexible (no fija); Solange lo ejecuta semanalmente en clientes de salud, dentro del módulo Conciliación.

## Arquitectura

**Sin build, sin dependencias locales, sin tests.** Son 4 archivos HTML completamente autónomos — cada uno contiene su CSS y JavaScript inline, y carga `@supabase/supabase-js@2` desde CDN (jsdelivr). No hay framework ni bundler. Para probar localmente basta servir los archivos estáticamente (ej. `python -m http.server`) o abrirlos en el navegador.

| Archivo | Módulo | Vista que lee | Tabla que escribe |
|---|---|---|---|
| `index.html` | Login + panel de módulos | — | — |
| `liquidaciones.html` | Ciclo liquidaciones/Previred | `v_checklist_mensual` | `ciclo_liquidaciones`, `clientes` |
| `conciliacion.html` | Conciliación SII/KAME + IVA salud | `v_checklist_conciliacion` | `ciclo_conciliacion`, `iva_salud_ejecucion`, `clientes` |
| `f29.html` | Ciclo F29 mensual | `v_checklist_f29` | `ciclo_f29` |

**Patrón de cada página:** lectura desde una vista Postgres (que ya junta cliente + ciclo + responsable + días restantes de plazo), filtrado/render en cliente sobre `estado.ciclos`, y edición vía modal que hace `update` directo a la tabla base. Tras guardar se recarga el dashboard completo.

**Código duplicado a propósito:** la configuración de Supabase (URL + publishable key), el flujo de auth (magic link), `formatFecha`, `formatMonto`, `escapeHtml` y `toast` están copiados en los 4 archivos. Cualquier cambio a esa lógica compartida debe replicarse en todos los HTML.

## Infraestructura

- **Supabase:** proyecto `nnwoknmbbxbjzswrkzmw` (plan Free, Postgres 17, us-east-1). Nota: el plan Free pausa el proyecto tras ~1 semana de inactividad — a futuro evaluar Pro (US$25) para producción.
- **GitHub:** `Cristiandot/RSTAXLEGAL`. Todo push a `main` redespliega AMBOS proyectos Vercel.
- **Vercel — sitio HTML (legacy):** proyecto `rstaxlegal-k3dl`, https://rstaxlegal-k3dl.vercel.app — sirve la RAÍZ del repo (los 4 HTML). Sigue siendo producción para Conciliación hasta que se migre.
- **Vercel — app Next (nuevo):** proyecto `rstaxlegal-panel`, https://rstaxlegal-panel.vercel.app — **Root Directory = `app`**, Framework Preset = Next.js. Es el panel nuevo (login + Liquidaciones + F29). Variables de entorno opcionales (la app tiene fallback público en `app/src/lib/supabase/config.ts`).
- **Auth / correos:** magic link de Supabase con **SMTP propio = Resend** (`smtp.resend.com`, user `resend`, password = API key). Dominio `rstaxlegal.cl` **verificado en Resend** (DKIM `resend._domainkey`, SPF + MX en subdominio `send`, cargados en el DNS de SiteGround — ver abajo). **Sender = `notificaciones@rstaxlegal.cl`**; el equipo completo recibe los links. (El correo incorporado de Supabase NO sirve para producción — rate limit; aplica igual en Free y Pro.)
- **DNS del dominio:** `rstaxlegal.cl` tiene los nameservers en **SiteGround** (`ns1/ns2.siteground.net`) — ahí se editan los registros (Site Tools → Domain → DNS Zone Editor). El **correo del equipo está en Microsoft 365** (MX raíz → `rstaxlegal-cl.mail.protection.outlook.com`); los registros de Resend van en el subdominio `send`, sin tocar ese MX.
- **Redirect URLs (Supabase Auth):** deben incluir `https://rstaxlegal-panel.vercel.app/**`, `https://rstaxlegal-k3dl.vercel.app/**` y `http://localhost:3000/**`. El **Site URL** sigue siendo el sitio viejo (`k3dl`); por eso, para aterrizar en el panel nuevo hay que **pedir el magic link DESDE la URL del panel** (si no, Supabase cae al Site URL = sitio viejo).

## Backend (Supabase)

Se administra vía MCP de Supabase — usar `apply_migration` para DDL y `execute_sql` para consultas. Nota: el servidor MCP local (`mcp__supabase`) requiere `SUPABASE_ACCESS_TOKEN`; si falla con "Unauthorized", usar el conector de claude.ai (`mcp__claude_ai_Supabase__*`, requiere `project_id`).

- **Tablas:** `usuarios`, `clientes`, `ciclo_liquidaciones`, `ciclo_f29`, `ciclo_conciliacion`, `iva_salud_ejecucion`, `checklist_implementacion`, `audit_log`, `reglamentos`. Todas con RLS habilitado.
- **Módulo RIHS (`/rihs`):** generador de Reglamento Interno de Higiene y Seguridad (D.S. N° 44, < 10 trabajadores) desde la plantilla `plantillas/GENERICO/RIHS DS44 (menos de 10 trabajadores).docx` (tag docxtemplater `{RAZON_SOCIAL}`). Guarda el .docx en el bucket privado `reglamentos` y registra la fila en la tabla `reglamentos`; el listado "Documentos generados" se descarga vía link firmado. La matriz de riesgos y los EPP por tarea se completan a mano por rubro. RIOHS queda pendiente.
- **Regla de documentos tipo (cartas/contratos/anexos/RIHS): NO citar el Código del Trabajo** (artículos) en el texto generado. Las causales y obligaciones se describen en palabras, sin "artículo N° … del Código del Trabajo". Aplicado en `lib/carta-aviso.ts` y en las plantillas de `plantillas/GENERICO/`. Decisión de Cristian (24-06-2026): las referencias normativas son innecesarias y pueden quedar mal; la revisión jurídica la hace el equipo.
- **Vistas:** `v_checklist_mensual`, `v_checklist_f29`, `v_checklist_conciliacion`, `v_historial`. Las vistas calculan campos derivados como `estado`, `dias_restantes_*` y `conciliacion_ok` — si se necesita un campo nuevo en el dashboard, normalmente se agrega a la vista, no al HTML.
- **Auditoría automática:** las tablas `clientes`, `ciclo_*`, `iva_salud_ejecucion` y `checklist_implementacion` tienen triggers AFTER INSERT/UPDATE/DELETE (`trg_audit_*`, SECURITY DEFINER) que registran en `audit_log` (vista `v_historial`), y triggers BEFORE UPDATE que mantienen `updated_at`. No es necesario (ni correcto) escribir en `audit_log` o `updated_at` desde el frontend.
- **Auth:** magic link (`signInWithOtp`) restringido a correos presentes y activos en la tabla `usuarios` (se valida antes de enviar el link y de nuevo al iniciar sesión). Roles en `usuarios.rol` (`admin`, `operador`).
- **Períodos:** columna `periodo` es texto con formato `YYYY-MM` (ej. `2026-05`). Se crea un ciclo por cliente por mes en cada tabla `ciclo_*`.
- **Arrastre histórico del responsable:** al INSERTAR un ciclo nuevo sin responsable, un trigger `BEFORE INSERT` lo hereda del período anterior más reciente del mismo cliente. Liquidaciones (`fn_ciclo_liq_hereda_responsable` / `trg_ciclo_liq_hereda_resp`) cae de respaldo a `clientes.responsable_default_id` si no hay ciclo previo; F29 (`fn_ciclo_f29_hereda_responsable` / `trg_ciclo_f29_hereda_resp`) solo mira ciclos F29 previos. Efecto: un cambio de responsable en un mes queda vigente de ahí en adelante sin re-asignar. No hay que setear responsable a mano al abrir un mes nuevo.

## Decisiones tomadas (no cambiar sin discutir)

- Plazo F29 uniforme día 20 (no diferenciar con/sin Previred).
- Responsable F29 separado de responsable Previred: al guardar responsable en ciclo F29 NO se hereda a `clientes.responsable_default_id`.
- ON/OFF del certificado KAME es por empresa (campo en `clientes`), no historial mensual.
- Soft delete de clientes (`activo=FALSE`), nunca DELETE físico.
- Empresas activas sin F29 ni Previred existen y son válidas (solo cartera).

## Objetivo inmediato (junio 2026): migración a Next.js

Migrar los 4 HTML standalone a un proyecto Next.js + TypeScript + Tailwind + shadcn/ui dentro de la MISMA carpeta del repo (subcarpeta `app/`), sin borrar los HTML. Stack: Next.js 16 App Router (se scaffoldeó con `create-next-app@latest`, que ya instala la 16; decisión explícita de quedarse en 16), TypeScript estricto, Tailwind v4, shadcn/ui, Supabase JS SDK oficial (`@supabase/ssr` con sesión por cookies). Durante toda la migración el sistema HTML actual sigue operando en Vercel; cuando los módulos React estén probados, se hace el switch del root del repo.

**Visión del proyecto `app/`:** no es solo migrar los 4 HTML, sino construir una **plataforma interna de operaciones** con dos tipos de módulos: (a) *ciclos* (Liquidaciones, Conciliación, F29 — registran estado de cumplimiento, ya existen como HTML) y (b) *gestiones / procesos guiados* nuevos (contrato de trabajo, finiquito, anexos, cartas) donde el trabajador carga lo mínimo y el sistema arma el documento desde la info que ya tiene del cliente. Supabase = fuente única de verdad. El **contrato de trabajo** es el arquetipo: el trabajador elige cliente/empresa → "Contrato nuevo" → el sistema precarga datos del empleador y pide solo los del empleado → merge con la plantilla tipo que cargó Felipe (abogado/admin) → documento que (1) se descarga, (2) queda en Supabase, y (3) da de alta al trabajador en el Excel compartido del cliente (Google Drive) que alimenta las liquidaciones. Datos personales (RUT, remuneración, domicilio) bajo **Ley 21.719 vigente** → RLS por rol desde el diseño.

**Roadmap:** Fase 0 shell (✓ hecho) · Fase 1 modelo de datos (`empresas`, `trabajadores`, `plantillas_documento`, `documentos_generados` + RLS) · Fase 2 módulo "Contrato nuevo" · Fase 3 integración Excel compartido (Google Sheets API) · Fase 4+ replicar patrón a otras gestiones y migrar ciclos.

**Estado del proyecto `app/` (al 09-06-2026):**
- **Stack/base (✓):** Next 16.2.7 App Router, TS estricto, Tailwind v4, shadcn/ui sobre **Base UI** (usa prop `render`, NO `asChild` de Radix), `@supabase/ssr` con sesión por cookies. Estructura con `src/` (rutas en `app/src/app/`). Env en `.env.local` (no versionado; ejemplo en `.env.example`).
- **Auth (✓):** login por magic link en `/login` con la MISMA validación contra `usuarios` (`correo` + `activo`) que el HTML, vía Server Action (`login/actions.ts`); callback en `/auth/callback` (soporta flujo PKCE `code` y `token_hash`); protección de rutas en `src/proxy.ts` (convención `proxy` de Next 16). Clientes Supabase en `src/lib/supabase/{client,server,middleware}.ts`; helper de sesión en `src/lib/auth.ts` (`getUsuarioActual`). Para login local: agregar `http://localhost:3000/**` a Redirect URLs en Supabase Auth.
- **Fase 0 — app-shell (✓):** sidebar colapsable (variante flotante) con secciones + menú de usuario (cierre de sesión vía cliente browser), topbar con fecha, layout autenticado en `src/app/(app)/layout.tsx`, home lanzador en `src/app/(app)/page.tsx` con tarjetas-stat de conteos reales de cartera. Todo se alimenta del **registro único de módulos** en `src/lib/modules.ts` (agregar módulo = una entrada ahí; estados `activo`/`desarrollo`/`proximamente`; visibilidad por rol).
- **Diseño (✓):** identidad corporativa extraída de rstaxlegal.cl — navy `#0B2545` (primario), teal `#17A2B8` (acento), slate `#34495E`; tipografía **Playfair Display** (títulos, util `font-heading`) + **Manrope** (cuerpo). Tokens y utilidades (`.stat-*`, `.card-soft`, gradiente del sidebar) en `src/app/globals.css`. Layout inspirado en un dashboard moderno (sidebar gradiente navy, tarjetas redondeadas, sombras suaves).
- **Liquidaciones (✓ migrado) y F29 (✓ migrado):** rutas `/liquidaciones` y `/f29` en `(app)`. Patrón: page servidor lee la vista (`v_checklist_mensual` / `v_checklist_f29`) + `usuarios` por período (searchParam `?periodo=YYYY-MM`, default = mes ANTERIOR al actual, porque lo que se trabaja/paga en un mes corresponde al período del mes previo —ej. en junio se trabaja mayo— vía `periodoPorDefecto()`) → client component (`*-client.tsx`) con resumen, filtros client-side, tabla y diálogo de edición → Server Action (`actions.ts`) hace el `update` con la sesión autenticada (RLS) y `revalidatePath`. Liquidaciones hereda responsable/modalidad a `clientes`; F29 NO (responsable independiente). Helpers compartidos: `src/lib/periodos.ts`, `src/lib/format.ts`, `src/lib/ciclos.ts` (tipos de vistas + clases de badges). Períodos generados dinámicamente (ya no hardcode 2026-05/06/07). **Estados de Liquidaciones (jun-2026, modelo simplificado):** 5 estados + "Sin iniciar". Pasos 1-3 se marcan con **checkbox inline en la tabla** (al tildar, `marcarPaso` estampa la fecha de hoy en la columna; sin elegir fecha): `En espera de detalle` (`fecha_consulta_enviada`), `Envío de liquidaciones` (`fecha_detalle_recibido`), `Envío previred` (`fecha_liquidaciones_enviadas`). Pasos 4-5 con **fecha en el modal**: `Previred listo para pago RS` (`fecha_previred_listo_pago`, + monto = `monto_previred_total`) y `Previred pagado` (`fecha_previred_pagado`, estado final). Se eliminó "Previred enviado"; `fecha_previred_presentada` quedó sin uso. El `estado` lo calcula la vista `v_checklist_mensual` por fechas; se edita vía `apply_migration` con el **conector claude.ai Supabase** (`mcp__ce9050fd...`), que SÍ funciona — el `mcp__supabase` local da Unauthorized.
- **Conciliación → CONTABILIDAD MENSUAL (✓ rediseño 11-06-2026):** ruta `/contabilidad` (`/conciliacion` = redirect). Contexto: Danilo (contador) trabaja para que KAME desaparezca; el módulo pasa de registrar fechas a SER el repositorio documental mensual. **Grilla principal** (filtro por mes `?periodo`): mini-checklist documental por empresa con siglas FC/FV/BV/BC (facturas compras/ventas, boletas ventas/compras) verdes si hay archivo cargado + chip "+N" de adicionales (honorarios, otros gastos/ingresos); cards y filtro por completitud (Sin documentos / Carga parcial / Facturas C+V); click en la fila → **detalle `/contabilidad/[clienteId]`**: resumen del cliente (estado, KAME, salud, responsable), CASILLAS de carga por categoría (subir/descargar/anular lógico — el archivo nunca se borra de Storage), formulario del ciclo (responsable, KAME al cierre → actualiza `clientes.kame_cert_*`, 3 fechas hito, observaciones; estado por fechas Sin iniciar → Descargando → Listo para conciliar → Conciliado), IVA salud (registrar/eliminar cambios) y gastos menores del portal con totales anuales. Catálogo de categorías en `contabilidad/categorias.ts` — sincronizar con el CHECK de `documentos_contables` (fact_compras, fact_ventas, boleta_ventas, boleta_compras, honorarios, otro_gasto, otro_ingreso, otro; las rcv_* se renombraron a fact_*). Subir facturas C/V estampa solo el hito si estaba vacío → el checklist de la grilla se mueve solo. Bucket privado `contabilidad` (`<cliente_id>/<periodo>/<categoria>-<ts>-<nombre>`); `bodySizeLimit 25mb` en next.config. Las actions usan `revalidatePath("/contabilidad", "layout")` para refrescar grilla y detalle. **Gastos menores (portal del cliente):** sección "Gastos e ingresos menores" en la portada del portal — compras/ventas con boleta que NO van al SII mensual pero SÍ a la Operación Renta; tabla `gastos_menores` (tipo compra/venta, fecha, descripción, monto, origen portal/equipo, anulación lógica, RLS+auditoría) con RPCs SECURITY DEFINER por `form_token` (`crear_gasto_menor`, `gastos_menores_de`, `anular_gasto_menor` — anon no toca tablas); UI portal en `solicitud/[token]/gastos-menores.tsx` + `gastos-actions.ts` (navegación por año). **Fase 2 — LIBROS RCV (✓ 12-06-2026, contabilidad completa):** Danilo entregó el traspaso técnico del proceso contable (doc `TRASPASO_TECNICO_CONTABILIDAD.md` en Downloads de Cristian; reglas de centralización 3.1-3.12, validado contra su workbook TEVICOMP 202601-202605 con D=H=$681.862.253). Pilotos con `clientes.hace_contabilidad_completa=true`: **Tevicomp, Inversiones Toro & Ortiz, Bahía Sargo**. Lo construido: (a) tablas `plan_cuentas` (cliente_id NULL = plantilla estándar RSTL de 19 cuentas seed; por-empresa con cliente_id; índices únicos parciales por código), `rcv_compras`, `rcv_ventas` (espejo del CSV del SII + `pagado_pct` default 100 + `cuenta_id` FK plan_cuentas; ÚNICA cliente+periodo+tipo_doc[+rut]+folio → reimportar NO duplica ni pisa pagado/cuenta porque el upsert no incluye esas columnas), `honorarios_periodo`; cols nuevas en `ciclo_f29` (`iva_debito, iva_credito, ppm, imp_unico, imp_2da_categoria, iva_postergado, fecha_pago_f29`). RLS estándar; auditoría en rcv_* solo UPDATE/DELETE (el INSERT masivo del CSV inundaría audit_log). (b) Parser `src/lib/contabilidad/rcv.ts`: resuelve columnas por NOMBRE de encabezado (el export real del SII trae col `Nro`, nombres distintos al consolidado del contador y SIN columna Periodo — el período va en el nombre de archivo, `periodoDesdeNombre`), separador `;`, UTF-8 con fallback Windows-1252. **REGLA DE SIGNO: las NC (60/61/112) se normalizan SIEMPRE a montos negativos** (el SII las exporta positivas; el workbook del contador las trae negativas) → la Σ simple del período calza con el F29 y la centralización invertirá DEBE/HABER sola. Verificado contra CSVs reales (compras 78073973-8 202501; ventas Toro & Ortiz 202505: 181 docs, 6 NC). (c) UI `/contabilidad/[clienteId]/rcv` (banner de acceso en el detalle del mes cuando el flag está activo): tabs Compras/Ventas, importar CSV (detecta libro por encabezados y período por nombre de archivo con confirmación si difiere del que se está viendo; deja copia del CSV en el checklist documental y estampa el hito de descarga), grilla con edición inline de % pagado y cuenta de gasto ("+ Nueva cuenta…" crea una cuenta de gasto de la empresa al vuelo y la asigna), totales del período, eliminar importación, y **validación RCV vs F29** (Σ IVA ventas = débito, Σ IVA recuperable compras = crédito, tolerancia $0) con formulario de montos del comprobante F29 que escribe en `ciclo_f29`. **Fase 3 pendiente (próximo paso):** motor de centralización de asientos según las reglas del traspaso de Danilo (tablas `asientos`+`asiento_lineas`; tipos TRASPASO/EGRESO/INGRESO; compras/ventas/pagos por %PAGADO, F29 con postergación art. 64 bis LIVS, remuneraciones por trabajador desde libros Previred, honorarios) + Libro Diario + Balance de Comprobación + Reporte gerencial mensual; export a Excel (decisión Cristian 12-06: el panel es la fuente oficial, NO se mantiene formato KAME). Validación de oro: reproducir el período 202601 de Tevicomp y cuadrar contra el workbook `TEVICOMP_2026_v9.xlsx` de Danilo.

## Módulo Contratos (Fase 1 — en construcción, piloto Bahía Sargo)

- **Visión:** solicitud (ficha digital) → generación automática del .docx desde plantilla aprobada → revisión → envío; luego alta en nómina + Excel compartido + alertas plazo fijo (fases siguientes). Piloto: **COMERCIAL BAHÍA SARGO LIMITADA (78.179.175-K)**, 2 labores: ATENDEDOR (isla) y ASTOP (minimarket ARAMCO), FT/PT, chileno/extranjero.
- **Modelo de datos (✓ aplicado 09/10-06-2026):** `clientes` + columnas empleador (representante_legal/_rut, domicilio, comuna, ciudad, giro, correo_empresa); tablas nuevas `trabajadores`, `contratos` (estado: solicitado→generado→aprobado→enviado | anulado; jornada y remuneración en jsonb), `plantillas_contrato` (matriz cliente × cargo × jornada × nacionalidad × tipo; flag `aprobada` — solo se genera desde plantillas aprobadas). RLS patrón estándar + triggers de auditoría/updated_at.
- **Plantillas fuente:** en `plantillas-fuente/` (13 .docx renombrados `SARGO 78179175-K - *` / `GENERICO - *`). **Ya traen placeholders `{VARIABLE}`** (~30 variables consistentes). Los horarios/turnos están como IMÁGENES incrustadas (mallas de turnos) — para el piloto van fijas por plantilla; genericizar después. Observaciones legales para Felipe: "chileno" hardcodeado en comparecencia (incluso versión extranjero), "En Curauma" y asig. caja $3.000 fijos, y el anexo PT 30h excede el tope de jornada parcial post Ley 21.561 (2/3 de 42h = 28h, Art. 40 bis CT).
- **Módulo construido (✓ 09-06-2026):** rutas `/contratos` (listado + flujo generado→aprobado→enviado→/anulado) y `/contratos/nuevo` (ficha de incorporación digital). Las 13 plantillas registradas en `plantillas_contrato` como **pre-aprobadas** (son los modelos en uso del abogado; formalizar aprobación cuando exista el módulo de plantillas). Plantillas .docx empaquetadas en `app/plantillas/` (incluidas en serverless vía `outputFileTracingIncludes` en next.config). Motor de merge: **docxtemplater** (`src/lib/generar-docx.ts`) — los placeholders `{VAR}` se rellenan con montos en palabras (`numero-palabras.ts`), fechas largas y RUT formateado (`rut.ts`); selección automática de plantilla por cliente × cargo × jornada × nacionalidad × tipo (horas exactas con fallback a genérica). Documento generado → bucket Storage `contratos` (privado, policies authenticated) → descarga por link firmado. El formulario completa los datos del empleador faltantes en `clientes` la primera vez.
- **Formulario público de solicitud (✓ 10-06-2026):** `/solicitud/[token]` — link tokenizado por cliente (col `clientes.form_token`), SIN login, con marca RSTL. El cliente llena la ficha y entra como contrato `solicitado`; el equipo le da "Generar" en `/contratos`. Escritura vía funciones **SECURITY DEFINER** (`solicitud_info`, `crear_solicitud_publica`) — el rol anon no toca tablas. Tarjetas "Copiar link de solicitud" por empresa en `/contratos`. Motor de generación compartido en `src/lib/contrato-generacion.ts`. (Reemplaza la idea de Microsoft Forms + Power Automate: el form propio valida RUT en origen y no requiere integración.) Datos del empleador Sargo cargados (rep. legal C. Valenzuela Milnes, domicilio Av. Cardenal Samore 2880 Curauma, comuna tributaria Limache, giro, correo). **Ajustes 10-06-2026 (reparos de Cristian):** (1) Isapre pide nombre + valor del plan en UF o $ (cols `trabajadores.salud_plan_unidad`/`salud_plan_valor`, RPC actualizada; el valor en es-CL se normaliza con `normalizarMonto` — coma decimal, punto miles); (2) las observaciones del contrato se movieron a la sección Remuneración como "Otras consideraciones remuneracionales" (mismo campo `observaciones`; para las demás gestiones sigue la tarjeta genérica abajo); (3) gratificación con 4 opciones estilo software de remuneraciones — Sin gratificación / 25% con tope / Tope (4,75 IMM ÷ 12) / Manual con campo de monto — guardada como texto legible en `remuneracion.gratificacion` + código en `gratificacion_tipo` ('sin'|'25'|'tope'|'manual') + `gratificacion_monto` numérico si manual; replicado en el form interno `/contratos/nuevo`. **La cláusula del Word se arma sola:** las 11 plantillas con cláusula de gratificación (en `app/plantillas/`) llevan el placeholder `{GRATIFICACION_TEXTO}` (parcheadas con `scripts/patch-gratificacion-plantillas.js`, que reemplaza texto repartido en runs); `contrato-generacion.ts::textoGratificacion()` redacta la cláusula según el tipo (solicitudes antiguas sin tipo → 25%). OJO: los .docx de `plantillas-fuente/` (los modelos de Felipe) conservan la redacción fija original — solo se parchearon las copias empaquetadas que usa la app. Redacciones de las 4 cláusulas pendientes de venia de Felipe (en especial "sin gratificación", que se redactó como remisión a los requisitos del Art. 47 CT para no pactar renuncia de un derecho irrenunciable).
- **Des-hardcodeo de plantillas SARGO (✓ 10-06-2026):** los datos variables que estaban escritos fijos en las 13 plantillas pasaron a placeholders (script `scripts/patch-placeholders-sargo.js`, idempotente): (1) asignación de pérdida de caja ("$ 3.000 Tres Mil" / "$XXXXX (en palabras)" / "$3.000 (tres mil pesos)") → `{ASIGNACION_CAJA_TEXTO}`, alimentado por `remuneracion.perdida_caja` (campo nuevo en portal y form interno; 0 → "No se pacta"); (2) lugar de firma "En (la ciudad de) Curauma" → `{CIUDAD_FIRMA}`, alimentado por col nueva `clientes.lugar_firma_contrato` (fallback ciudad → comuna; Sargo = 'Curauma', puede diferir de la comuna tributaria); (3) "chileno" fijo en la comparecencia (incluso versiones extranjero) se eliminó — la nacionalidad ya sale de `{NACIONALIDAD_EMPLEADO}`. **Horas parametrizadas (✓ 10-06-2026):** la cláusula de jornada de los 11 contratos usa `{HORAS_SEMANALES}` (motor: completa → 42, parcial → horas pactadas). Esto corrigió de paso 4 plantillas que decían "44 horas semanales" — tope derogado desde el 26-04-2026 (Ley 21.561: 42 hrs; bajará a 40 el 26-04-2028, ajustar el default del motor entonces). Cuidado al parchear .docx: hay espacios duros (NBSP, 0xA0) entre palabras — usar `[\s ]` en las regex. **Duplicado ATENDEDOR PT resuelto (10-06-2026):** la "ATENDEDOR PT 21h" resultó ser el contrato aStop completo con el título cambiado (150/153 párrafos idénticos al borrador ASTOP PT: protocolo "bienvenido a aStop", parrillas/coolers, estándares de imagen aStop) — desactivada en `plantillas_contrato` (`activo=false`, archivo intacto en repo). Queda como única PT de Atendedor la redacción de pista (horas=null, sirve cualquier jornada parcial con `{HORAS_SEMANALES}`). Fundamento: Art. 10 N°3 CT — las funciones pactadas deben corresponder a la naturaleza real de los servicios. **Pendiente Felipe:** redactar la versión moderna del contrato de Atendedor de pista (estructura moderna + funciones de isla) si quiere homologar; la redacción moderna además salta de VIGÉSIMO QUINTO a VIGÉSIMO OCTAVO (revisar también en los borradores ASTOP, que comparten el texto).
- **Flujo futuro de onboarding de plantillas de otros clientes:** Cristian entregará contratos ejemplo con datos reales de un trabajador; el proceso es identificar todo dato variable (nombres, RUT, montos, fechas, lugar, nacionalidad), convertirlo a los placeholders estándar (~30, ver plantillas existentes) con el mecanismo de reemplazo cross-run de los scripts en `scripts/`, registrar en `plantillas_contrato` y verificar con render de prueba.
- **Portal de solicitudes ampliado (✓ 10-06-2026):** el link público es ahora un portal de **5 gestiones**: contrato nuevo, anexo, **carta de amonestación**, **finiquito/cálculo de despido** (solo solicitud de datos: fechas ingreso/término, causal en lenguaje simple mapeada a Art. 159/160/161 CT, sueldo + otros pagos promedio, días de vacaciones tomados —dato que de momento declara el cliente—, aviso 30 días, licencia vigente, fueros; con advertencia de no comunicar el despido antes de la revisión legal) y **papeleta de vacaciones**. Las 3 nuevas van a la tabla `solicitudes_rrhh` (RLS+auditoría, RPC `crear_gestion_rrhh` SECURITY DEFINER) y se gestionan en el **módulo `/gestiones`** (bandeja con filtros, detalle legible con alertas de fuero, flujo solicitada→aprobada→enviada/rechazada; el envío del resultado al `correo_contacto` es manual por ahora — el envío automático vía Resend API requiere cargar la API key como env var). **Carta de amonestación con generación automática (✓ 11-06-2026):** portal con catálogo de 10 motivos estándar + "Otro" (`lib/amonestaciones.ts`; se eliminó la pregunta de amonestaciones previas); plantilla genérica para todas las empresas en `app/plantillas/GENERICO/CARTA Amonestacion.docx` (creada con `scripts/crear-carta-amonestacion.js`, Courier New; redacción propia → venia de Felipe pendiente); motor en `lib/carta-amonestacion.ts` redacta el párrafo de hechos según el motivo ({MOTIVO_TEXTO}), botón "Carta"/"Generar carta" en `/gestiones` sube el .docx a Storage (`contratos/gestiones/<id>/`) y descarga con link firmado; col nueva `solicitudes_rrhh.documento_path`. Las plantillas se incluyeron en el bundle de `/gestiones` (next.config). **Redacción IA de hechos (✓ código 11-06-2026, ⏳ requiere env var):** al generar la carta, la descripción del cliente pasa por Claude (`claude-opus-4-8` vía `@anthropic-ai/sdk`, `lib/redactar-hechos.ts`) que la reescribe formal en tercera persona sin inventar hechos (conserva fechas/nombres/montos; el original queda en `datos.descripcion`). Fallback silencioso al texto original si falta `ANTHROPIC_API_KEY` o la llamada falla. **Pendiente Cristian: cargar `ANTHROPIC_API_KEY` en Vercel** (console.anthropic.com → key → Vercel env vars → redeploy), ídem `RESEND_API_KEY`.
- **ASTOP FT Extranjero reconstruido (✓ 11-06-2026):** la versión en uso era la redacción antigua corta sin cláusulas modernas de tienda; ahora = ASTOP FT moderno + bloque VIGÉSIMO NOVENO de extranjero (visa, Ley 18.156, impuesto >13,5 UTM). Script reutilizable `scripts/construir-extranjero-desde-moderno.js` (base chileno + fuente con bloque extranjero → salida); sirve para construir las versiones extranjero que faltan. Antigua respaldada en `plantillas-desactivadas/`. Para venia de Felipe.
- **Pendiente módulo Contratos/Gestiones:** probar end-to-end con un contrato real de Sargo; generación automática del Word de **anexos**; **envío por correo con un click** (Resend API); plantillas extranjero faltantes (Atendedor PT, aStop indefinido/PT — ofrecido generar borradores para aprobación de Felipe); módulo admin de plantillas; Fase 3 = alta en nómina + Excel compartido + alertas vencimiento plazo fijo. La col `clientes.form_solicitud_url` (MS Forms) quedó sin uso.

## Módulo Indicadores Previred (✓ 11-06-2026)

- **Ruta `/indicadores`:** cada mes se sube la hoja PDF "Indicadores Previsionales" de Previred; el sistema la parsea, muestra los valores en un diálogo de revisión editable y guarda en la tabla `indicadores_previred` (upsert por período, RLS + auditoría estándar). El PDF original queda en el bucket privado `indicadores` (`previred/<periodo>.pdf`). Base para cálculos de remuneraciones y finiquitos.
- **Convención de período:** `periodo` = mes de REMUNERACIONES, consistente con el resto del panel. La hoja "Para Cotizaciones a Pagar en Junio 2026 (Remuneraciones Mayo 2026)" → `periodo='2026-05'`; el mes de pago se deriva (período + 1) y se guarda en `datos.mes_pago`.
- **Parser (`src/lib/previred.ts`, motor `unpdf`):** pdfjs entrega el texto en orden de dibujo, así que se parsea por COORDENADAS — ítems agrupados en filas visuales (Y ± 4) y para cada etiqueta se toma el primer número a su derecha. Extrae UF (último día mes y mes anterior), UTM/UTA, topes imponibles AFP/INP/AFC (CLP + N° UF de la etiqueta), rentas mínimas (4), tasas AFP por administradora (jsonb, detección dinámica de filas — soporta nuevas AFP), SIS, seguro social "expectativa de vida", distribución 7% salud CCAF/FONASA, AFC por tipo de contrato, trabajos pesados, topes APV/depósito convenido y tramos de asignación familiar (con desde/hasta parseados de la glosa). Campos ilegibles → `null` + lista `advertencias` que el diálogo muestra para completar a mano. Verificado 100% sin advertencias contra la hoja de mayo 2026.
- **Escalares editables en el diálogo de revisión; las tablas jsonb (AFP/AFC/asignación) se guardan tal como se leyeron** (se muestran resumidas para chequeo visual). "Editar valores" reabre el diálogo sobre un período ya guardado.

## Módulo Finiquitos (✓ 11-06-2026)

- **Ruta `/finiquitos`:** bandeja con las solicitudes de término que llegan del portal del cliente (`solicitudes_rrhh` tipo `finiquito`) + **calculadora de finiquito** en `/finiquitos/calculadora` (con `?gestion=<id>` se prellena desde la solicitud; sin parámetro es cálculo libre). Reemplaza el uso de calcular.cl. El flujo de estados (aprobar/enviar) sigue en `/gestiones` — acá solo se calcula.
- **Prellenado:** causal/fechas/aviso de la solicitud del portal; fecha de ingreso y sueldo base del último contrato del trabajador (si vino con `trabajador_id`); **UF e IMM desde `indicadores_previred`** del mes ANTERIOR a la fecha de término (Art. 172; fallback al período más reciente disponible, override manual de UF en la UI). Alertas si el cliente declaró fuero o licencia vigente.
- **Motor (`src/lib/finiquito.ts`, puro, corre en el navegador):** catálogo de causales 159/160/161/163 bis (`CAUSALES_FINIQUITO`, mapeo portal→código en `CAUSAL_PORTAL_A_CODIGO`); indemnización años de servicio (fracción >6 meses, tope 11 años post-14-08-1981), sustitutiva del aviso previo, base Art. 172 con tope 90 UF (colación/movilización incluibles vía checkbox — criterio CS vs DT), feriado pendiente + proporcional (15 ó 20 días hábiles/año zona extrema, conversión hábiles→corridos con el calendario de `lib/feriados.ts`), gratificación legal Art. 50 (25% tope 4,75 IMM/12 con el IMM del período), descuento aporte AFC, y `notas` legales contextuales (Ley 21.122 obra/faena, feriado progresivo Art. 68, lucro cesante plazo fijo, plazo 10 días hábiles Art. 177). Verificado con casos de control (3a8m→4 años; tope 11 años × 90 UF; renuncia sin indemnizaciones).
- **Guardar cálculo:** botón en la calculadora persiste `{entrada, resumen, calculado_en}` en `datos.calculo_finiquito` de la solicitud (action `guardarCalculoFiniquito`); la bandeja muestra el total calculado. Pendiente natural: generar el Word del finiquito desde el cálculo aprobado.
- **Export CSV carga masiva DT (✓ 11-06-2026):** checkboxes en la bandeja (solo con cálculo guardado) + botón "CSV DT (n)" → genera el archivo de carga masiva del **Finiquito Laboral Electrónico de Mi DT** con el formato exacto de la plantilla oficial (instructivo v4.0 abril 2026, en Downloads de Cristian): 48 columnas `;`, RUT sin puntos con guion, fechas dd-mm-aaaa, montos enteros CLP, causales en códigos DT (tabla en `lib/dt-finiquitos.ts`: 161 inc.1°=18, 161 inc.2°=19, 163bis=20, 159 N°1-6=3-8, 160 N°2-7=11-16, 160 N°1 letras=24-29), región derivada del código de comuna, `DescuentoAfc=0` (lo calcula la DT), máx 100 filas. Montos desde `calculo_finiquito.resumen`; funciones/correo/comuna/dirección/banco/cuenta desde `trabajadores`. Comunas: codificación ANTIGUA de la DT (Quilpué 5106, Limache 5505 — NO el CUT de Marga Marga), catálogo V Región + RM en la lib (agregar al aparecer comunas nuevas). Si faltan obligatorios (típico: email y datos bancarios), el CSV igual se descarga y un diálogo lista qué completar por trabajador. **Correcciones tras primera carga real rechazada por la DT:** SIN BOM (el validador lo lee pegado a "RutEmpresa" y da la columna por vacía), TODOS los montos con 0 explícito (la DT rechaza vacíos), advertencia si la fecha de término es futura (la DT solo acepta relaciones ya terminadas, fecha en zona Chile), y `RutEmpresa` debe ser el del empleador LOGUEADO en Mi DT. Además botón "CSV DT" POR FILA (un solo finiquito, sin checkboxes). **Datos de pago del trabajador (✓ 11-06-2026):** el portal los pide en la solicitud de finiquito (correo personal + banco/tipo cuenta/N° de cuenta UNIPERSONAL, opcionales, catálogos DT); en el panel se editan con el botón "Banco" de la bandeja (ámbar si faltan; diálogo rápido) o en la tarjeta "Datos de pago" de la calculadora — action `guardarDatosPago` escribe en la ficha del trabajador (si vinculado) + copia en `datos` de la solicitud; el export CSV usa ficha con fallback al portal.
- **Carta de aviso de término Art. 162 (✓ 11-06-2026):** botón "Generar carta de aviso" en la calculadora (causales 159 N°4-6, 160, 161, 163 bis — NO renuncia/mutuo acuerdo/muerte). Diálogo: fecha de entrega, fecha efectiva (sincronizada con el cálculo), forma de entrega (personal con recepción al pie / correo certificado al domicilio — precargado desde `trabajadores.direccion`), hechos que fundan la causal (texto literal, **venia de Felipe pendiente** como toda redacción), y mes de cotizaciones pagadas (Ley Bustos). La carta lleva el detalle de indemnizaciones del cálculo vigente + total (Art. 162 inc. 4°), párrafo de aviso previo según corresponda, puesta a disposición del finiquito (Art. 177) y mención de copia a la DT. Plantilla `app/plantillas/GENERICO/CARTA Aviso Termino.docx` (regenerable con `scripts/crear-carta-aviso.js`), motor `lib/carta-aviso.ts` → Storage `contratos/gestiones/<id>/carta-aviso-termino.docx` + referencia en `datos.carta_aviso`. La calculadora además precarga desde `datos` de la solicitud: `vacaciones_dias_obtenidos` (saldo devengado según control del cliente — evita que el automático 15×años sobrepague) y `remuneracion_pendiente` (líquido).
- **CRITERIO RSTL (Cristian, 11-06-2026): la "remuneración pendiente" se ingresa SIEMPRE en LÍQUIDO** (neto tras AFP/salud del trabajador; las cotizaciones se declaran vía Previred del mes, no en el finiquito). Así el total del finiquito = lo que se paga al trabajador. Feriado proporcional e indemnizaciones van íntegros (no imponibles). Primer caso real: Rojas Briones (Distribuidora Barrera) — renuncia 12-06-2026, gestión registrada con cálculo guardado.
- **CRITERIO RSTL (Cristian, 15-06-2026): las cartas de aviso (y los finiquitos) SIEMPRE se fundan y muestran la fecha de inicio del CONTRATO ORIGINAL (antigüedad reconocida), no la de un anexo, renovación o paso a indefinido.** La carta Art. 162 incluye la cláusula "...cuya relación laboral se inició con fecha {FECHA_INGRESO}..."; `lib/carta-aviso.ts` toma `FECHA_INGRESO` de `datos.calculo_finiquito.entrada.fechaInicio` (la misma fecha del cálculo de indemnización), con fallback a `datos.fecha_ingreso`. Implica que `trabajadores.fecha_ingreso` debe contener el inicio original (ej.: el plazo fijo previo, no la conversión a indefinido). Primer caso: Cortés Godoy (Distribuidora Barrera) ingreso 01-11-1995.
- **REGLA RSTL (Cristian, 12-06-2026): en contratos a PLAZO FIJO el trabajador NO tiene descuento de seguro de cesantía (AFC).** El 0,6% del trabajador aplica SOLO a contratos indefinidos; en plazo fijo el 3% lo paga íntegro el empleador (Ley 19.728 Art. 5). Vale para TODO cálculo del panel (liquidación ejemplo, futuras liquidaciones, simuladores): si `tipo_contrato = 'plazo_fijo'` → AFC trabajador = $0. Ya implementado en `lib/liquidacion-ejemplo.ts` (no confundir con el "descuento AFC" del finiquito, que es el aporte del EMPLEADOR imputable a la indemnización Art. 161 — Art. 13 Ley 19.728).

## Portal del cliente y gestiones por módulo (✓ 11-06-2026 — reemplaza las referencias a `/gestiones` de secciones anteriores)

- **Portada del portal:** `/solicitud/[token]` ya no aterriza en el formulario — muestra una vista general (`portal.tsx`): razón social, conteo de trabajadores registrados, y dos accesos: **Panel de solicitudes** (el formulario de gestiones) y **Detalle remuneraciones** (ver abajo).
- **Desplegable de trabajadores (✓):** para toda gestión ≠ contrato nuevo, el cliente elige al trabajador de la lista de registrados (activos o con `updated_at` < 180 días — cubre plazo fijo caducado aún vinculado), con respaldo "Otro trabajador" manual. `solicitud_info` devuelve `trabajadores` (id, nombre, rut, cargo y fechas del último contrato — precargan fecha de ingreso del finiquito y fecha/cargo del anexo). `crear_gestion_rrhh` acepta `trabajador_id`, valida pertenencia y toma nombre/RUT canónicos de la BD; col `solicitudes_rrhh.trabajador_id` (FK). El anexo con trabajador seleccionado REUTILIZA la fila de `trabajadores` (antes duplicaba). **Cols contractuales en `trabajadores` (✓ 11-06-2026):** `cargo`, `sucursal`, `tipo_contrato`, `fecha_ingreso`, `fecha_termino_contrato`, `sueldo_base` — para trabajadores cargados desde fuentes externas (KAME/liquidaciones) sin filas en `contratos`; `solicitud_info` hace fallback a ellas (coalesce con el último contrato). El sueldo NO se expone en la RPC pública. **Primera carga masiva:** 85 trabajadores de DISTRIBUIDORA BARRERA Y CIA. LTDA. (cruce listado KAME × liquidaciones mayo 2026; 83 activos, 2 con contrato vencido inactivos; detalle en la memoria del cliente A.4).
- **Finiquito simplificado (✓):** el cliente ya NO entrega datos de cálculo (se eliminaron sueldo, vacaciones tomadas, licencia, fuero): solo trabajador, causal, **fecha de aviso** y **fecha de término** (`datos.fecha_aviso`/`fecha_termino`); si la causal es Art. 161, elige `aviso_modalidad` ('avisar_30'|'pagar_mes', Art. 162 inc. 4°). Nota UI: aviso y término pueden ser el mismo día. Las solicitudes antiguas conservan sus campos y la bandeja los muestra solo si vienen.
- **Vacaciones por rango (✓):** sin campo de días — el cliente pone primer y último día y ve en vivo los días hábiles consumidos + fecha de reintegro. **`src/lib/feriados.ts`**: tabla de feriados legales de Chile 2026-2028 (traslados Ley 19.668, Ley 20.299, solsticio Ley 21.357) + `calcularVacaciones()` (sábado SIEMPRE inhábil, Art. 69 CT). El server recalcula (no confía en el navegador) y guarda `dias_habiles` + `fecha_regreso`; rango fuera de la tabla → flag `cobertura_feriados='incompleta'` (⚠ en bandeja). **Extender la tabla antes de fines de 2028.**
- **Permisos (✓):** sexta gestión del portal. Catálogo común a todos los clientes en **`src/lib/permisos.ts`** (estructura traída del sistema Red Barrera, sin sus datos): con/sin goce, día administrativo, matrimonio/AUC Art. 207 bis, nacimiento Art. 195, fallecimiento Art. 66, médico, judicial, compensación de horas extras, otro. El goce NO se pregunta: lo fija el catálogo (bloqueado en los legales) y una nota explica el efecto (sin goce = se descuenta de remuneración, no de vacaciones); tipos convencionales invitan a aclarar pactos distintos en observaciones. Unidad: días completos (guarda `dias_corridos` + `dias_habiles` referencial; se permite sábado/domingo) u horas dentro de un día. No descuentan feriado.
- **Módulos por gestión (✓):** se eliminó el módulo único "Gestiones RRHH" (`/gestiones` ya no existe como ruta). Cada gestión tiene bandeja propia en el sidebar: **`/anexos`** (reutiliza `ContratosClient` con `mostrarHerramientasContrato=false`), **`/amonestaciones`** (con generación de carta), **`/vacaciones`**, **`/permisos`** — las 3 últimas reutilizan `GestionesClient` parametrizado (`titulo`, `descripcion`, `tipoFijo`); el código compartido sigue en `src/app/(app)/gestiones/` (client + actions, sin page). Finiquitos ya tenía módulo propio.
- **Detalle remuneraciones (✓):** segunda sección del portal, modelada desde el Excel compartido de GSOEX (B.5): el cliente carga por período las **novedades del mes que NO viajan por gestiones** — horas extra, horas en feriado/domingo, licencias médicas, anticipos, bonos, descuentos (catálogo en **`src/lib/novedades.ts`**) — y ve de solo lectura los permisos/vacaciones/finiquitos del mes ya pedidos por el panel (**cero re-digitación**, decisión explícita). Botón "Cerrar mes y enviar a RSTL" → período inmutable para el cliente. Tablas `novedades_remuneraciones` (tipos con CHECK; `origen` 'cliente'|'equipo') y `periodos_remuneraciones` (unique cliente×período, estado abierto|cerrado), RLS + auditoría estándar; RPCs SECURITY DEFINER `remuneraciones_info`, `guardar_novedad_remuneracion`, `eliminar_novedad_remuneracion`, `cerrar_periodo_remuneraciones`. Cols `clientes.recargo_feriado` (0.5) y `recargo_domingo` (0.3) para el cálculo del equipo. Visión: el Excel de Google Drive pasa de ser entrada del cliente a ser SALIDA generada desde Supabase. **Ajustes 11-06-2026 (reparos de Cristian):** (a) calendario del mes bajo el selector de período con feriados de Chile — irrenunciables del comercio EN ROJO (Ley 19.973: 01-01, 01-05, 18/19-09, 25-12; `FERIADOS_IRRENUNCIABLES_MM_DD` + `feriadosDelMes()` en `lib/feriados.ts`), resto celeste, domingos tenues, leyenda + lista nominada; (b) **licencia médica fuera del desplegable del portal** (flag `enPortal:false` en el catálogo — sigue para historial y equipo); (c) labels feriado/domingo con "(si aplica)" + hint de que el recargo depende del contrato; (d) **historial del mes editable** bajo el formulario: lápiz carga la novedad en el form en modo corrección (RPC `actualizar_novedad_remuneracion`, solo origen cliente + mes abierto; el trabajador no se cambia — eliminar y recrear) o basurero elimina.
- **Módulo Excel compartidos (✓ 11-06-2026):** ruta `/excel` (sección Cartera, activado). Vista interna en vivo por período (`?periodo=`, default mes actual) y empresa: estado del mes (badge), gestiones del panel que caen en el mes (misma regla de fechas que la RPC del portal, filtrada en JS), y novedades con badge de `origen` cliente/equipo. El equipo agrega novedades (form plegable, `origen='equipo'`), elimina cualquiera, y **cierra/REABRE el mes** del cliente (el cliente solo cierra; reabrir es exclusivo del equipo, upsert directo con sesión autenticada). Botones por empresa: **"Generar documentos liquidaciones"** (serán los PDF de liquidaciones que se envían al cliente para revisión — NO un Excel), **"Enviar cliente"** (usa `clientes.correo_empresa`, visible en la tarjeta; advierte si falta) y **"Generar archivo Previred"**. Los tres son placeholders con toast: la generación queda pendiente de las **reglas que entregará Cristian** (formato PDF liquidaciones + reglas del archivo Previred) y el envío además de `RESEND_API_KEY`. Flujo de fin de mes: Cerrar mes → Generar documentos → Enviar cliente → Generar Previred. Actions del módulo en `(app)/excel/actions.ts`; las actions compartidas de bandejas (`(app)/gestiones/actions.ts`) revalidan las 4 rutas (`revalidarBandejas()`).

## Módulo Facturación (✓ 12-06-2026)

Ruta `/facturacion` (sección "Oficina" del sidebar, bajo Links clientes): facturas emitidas por la oficina a los clientes. **Archivo histórico 2026 migrado completo** desde OneDrive `RSTAXLEGAL/Administración/Facturación/2026/NN. Mes/` — 560 documentos (547 facturas + 13 notas de crédito, enero a junio), cuadrados 1:1 contra las carpetas. Tabla `facturas` (folio único por tipo factura|nota_credito, `folio_ref` = factura referenciada por la NC, periodo, monto opcional, RLS + auditoría estándar) + bucket privado `facturas` (PDFs en `2026/<folio>.pdf` y `2026/nc-<folio>.pdf`). 498 vinculadas a `clientes` (match exacto normalizado + difuso pg_trgm curado a mano — la BD usa alias tipo "MELV SpA - …", "(HEYDI)"); 62 sin vincular = receptores externos reales (sindicatos, corporaciones, terceros) — se muestran por razón social y se pueden vincular desde la grilla. Funciones: filtros mes/tipo/vínculo/pago + búsqueda folio/cliente, descarga con link firmado y nombre legible, **"Enviar"** por correo (Resend, PDF adjunto, a nombre del usuario conectado; requiere RESEND_API_KEY), **carga masiva** ("Cargar facturas del mes": el mes + todos los PDFs de una vez, en tandas <3MB por el límite de ~4.5MB de Vercel; de cada PDF se extraen monto TOTAL y RUT receptor vía unpdf y el cliente se vincula por RUT con fallback razón normalizada; NC reconocidas por nombre `NC (folio) - …`; reporte por archivo y anti-duplicados por folio) y vinculación manual. **Estado de pago por factura** (`pagada`/`fecha_pago`): badge clickeable → diálogo con checkbox Pagada + fecha real elegible Y la **suscripción del cliente** en el mismo diálogo (`clientes.suscripcion_pago` + `suscripcion_dia_pago` opcional — hay suscritos sin día conocido, ej. MTN GOLD): badge VERDE "Suscripción · día N" para no pagadas de suscritos (estado deseable ≠ Pendiente; aplica automáticamente a las facturas de los meses siguientes por ser atributo del cliente). **Backfill ejecutado 12-06**: monto y `rut_receptor` extraídos de los 560 PDFs históricos (560/560), vinculación final 532/560 (28 externos genuinos). Scripts reutilizables: `migrar-facturas.js` (migración por carpeta, para 2027) y `extraer-datos-facturas.js` (montos/RUT). Notas: NC 65 de enero venía rotulada "NC (64)" duplicada — folio reasignado por secuencia, en observaciones; factura 1011 es de un "Almacenes Disfruto" homónimo con RUT distinto al de cartera — desvinculada con observación; facturas a "CLAUDIO FUENTES FERNANDEZ IMPORTACIONES Y VENTAS EIRL" resueltas por RUT.

## Sesión portal/F29 (11/12-06-2026) — resto de avances

- **Links clientes (✓):** ruta `/links`, sección "Portal clientes" al final del sidebar. Grilla jerárquica **cliente-matriz → empresas** (tabla nueva `grupos_cliente` código+nombre, ej. A.4 Red Barrera; col `clientes.grupo_id`): copiar/abrir el link del portal por empresa, **generar token** para las que no tienen (nunca regenera uno existente — invalidaría el link compartido), crear grupos y asignar empresas inline. Los links de solicitud se ELIMINARON de `/contratos` — la grilla es el único lugar. Base para futuros accesos por cliente (varias empresas bajo un acceso).
- **Blindaje RUT (✓ auditoría 11-06: 0 duplicados, 222 DV válidos):** índices únicos `uq_clientes_rut_empresa` (RUT normalizado, global) y `uq_trabajadores_cliente_rut` (por empresa — la misma persona puede estar en 2 empresas). `previred_rut` SÍ se repite a propósito (6 grupos). `crear_solicitud_publica` hace UPSERT del trabajador por RUT (reutiliza y actualiza la ficha en vez de duplicar o chocar con el índice).
- **Liquidación ejemplo en Contratos (✓):** botón por fila (ícono boleta) → diálogo con el líquido de un mes normal: gratificación según tipo pactado, **tasa real de la AFP del trabajador desde `indicadores_previred`**, salud 7%, AFC 0,6% solo indefinidos, impuesto único por tramos UTM, asignaciones no imponibles. Motor puro `lib/liquidacion-ejemplo.ts` (acepta `dias` para pago por día). **Criterio de pacto por líquido** (caso María José Mardones, C.6): base = mínimo proporcional por horas (reajustable IMM) + colación/movilización como ajuste fino → líquido exacto al peso.
- **Descargas sin tildes (✓):** `nombreArchivo()` en `lib/format.ts` — TODOS los nombres de archivo descargados/adjuntos van en MAYÚSCULA sin tildes (los acentos en Content-Disposition llegaban percent-encodeados como "Mar%C3%ADa"). Aplicado en contratos, cartas, facturas y documentos contables. El CONTENIDO de los documentos conserva tildes.
- **Correo a nombre del usuario conectado (✓):** `enviarCorreo` acepta `de` (getUsuarioActual) — sale "Cristian López <clopez@rstaxlegal.cl>" con reply-to y copia oculta al remitente (registro en su buzón M365). Fallback notificaciones@. Dominio completo verificado en Resend.
- **Saneamiento linter Supabase (✓ 11-06):** las 4 vistas `v_checklist_*`/`v_historial` ahora son **security_invoker** (⚠ al recrear una vista, volver a ejecutar `ALTER VIEW … SET (security_invoker = on)`); search_path fijado en `fn_set_updated_at`/`handle_new_auth_user`; políticas SELECT redundantes eliminadas; RPCs del portal con GRANT explícito a anon+authenticated (revocado PUBLIC). Restantes esperados: RLS always-true (diseño equipo completo) y RPCs anon (token). "Leaked password protection" = Pro-only e irrelevante (solo magic links).
- **F29 ágil (✓ 12-06):** patrón Liquidaciones en la grilla — **checkbox único "F29"** (estampa `fecha_f29_presentado` → Cerrado; armado quedó solo en el modal), selector inline **quién paga** (col nueva `ciclo_f29.pago_por` 'rs'|'cliente' con CHECK, en `v_checklist_f29` recreada) y **monto editable en la fila** (guarda al salir/Enter). Actions `marcarPasoF29` y `actualizarPagoF29`. **Modal sin fechas (✓ 12-06):** el modal muestra UN solo checkbox "F29" (mismo hito que la grilla, `fecha_f29_presentado`) — tildar estampa la fecha de hoy al guardar (conserva la original si ya estaba marcado), destildar la borra; ya no se eligen fechas en ninguna parte del módulo. `fecha_f29_armado` dejó de gestionarse en la UI (se conserva tal cual en BD).
- **Eliminación con doble seguridad en Contratos/Anexos (✓):** botón solo admins (RLS "Admins borran contratos" la exige en BD) + diálogo que obliga a escribir "borrar"; borra fila + documento de Storage; el diálogo recuerda que "Anular" conserva el registro.
- **Datos cargados:** Distribuidora Barrera (85.274.100-7) con datos SII + **85 trabajadores** (cruce KAME × liquidaciones mayo; detalle en memoria A.4); grupo C.6 Diego Segovia: 3 empresas con links + datos SII (More Than Meat SIN rep. legal — crítico, ver memoria C.6) + plantillas contrato "Atención del Local" (`plantillas/SEGOVIA C6`, ambas sociedades) + contrato María José Mardones (borrador con jornada en amarillo, pendiente horario del cliente); LUCUMO SPA con datos SII. Finiquito Rojas Briones (Barrera) registrado en `/finiquitos` con cálculo guardado (líquido $272.262, plazo Art. 177: 23-06).

## Módulo Onboarding y Calidad de Datos (✓ modelo 29-06-2026 — Paso 1)

Objetivo: completar y dejar limpios los datos de TEXTO de empresas y trabajadores (migración desde KAME con campos incompletos), y que el sistema se mantenga solo. Prioridad datos estructurados, no documentación. Vive en ambos dashboards: onboarding pre-llenado en el portal del cliente (confirma/corrige, no escribe de cero) y panel interno de control por campo + cola de validación. **Principios (respetar):** pre-llenado no formularios en blanco; **fuente por campo** (SII/KAME/CLIENTE); todo estructurable en **selector cerrado**; **RUT validado con DV al ingreso** (llave natural anti-duplicados); **gate progresivo** (el cliente entra igual, se bloquea solo lo que depende del dato faltante); separar inmutables (RUT, inicio actividades) de mutables; carga a producción **solo tras validación** (staging).

- **Reutiliza `clientes` y `trabajadores` como tablas productivas** (NO se crearon tablas paralelas — ya eran el superset). Hallazgo del análisis de llenado (142→132 trab.): el núcleo previsional/laboral viene casi completo de KAME (RUT/AFP/salud/cargo/tipo contrato/fecha ingreso/sueldo 92-100%); lo que falta es casi todo **CLIENTE** (sexo, jornada, datos bancarios, estado civil, nacionalidad, fecha nac., dirección/comuna, correo: 2-21%).
- **Modelo (migraciones aplicadas vía conector claude.ai Supabase):** (a) columnas nuevas en `clientes`: `tipo_sociedad`, `nombre_fantasia`, `regimen_tributario`, `fecha_inicio_actividades` (inmutable, ≠ `fecha_inicio_servicio`), `actividades_sii` jsonb, `socios` jsonb, `banco`/`tipo_cuenta`/`numero_cuenta`, `onboarding_estado` (`pendiente_contacto→invitado→en_proceso→en_revision→completo`, con CHECK) + fechas hito. (b) `catalogo_valores` (tipo, codigo=valor canónico almacenado, etiqueta, orden, extra jsonb p/ región de comunas) y `catalogo_variantes` (tipo, variante=`fn_norm_texto` del valor sucio, codigo) — selectores: afp, salud, isapre, banco, tipo_cuenta, estado_civil, sexo, regimen_tributario, tipo_sociedad, tipo_contrato, jornada_tipo, comuna. **`salud` guarda la INSTITUCIÓN real (Fonasa o isapre específica), no Fonasa/Isapre genérico** → plan_isapre obligatorio si `salud<>Fonasa`. (c) `onboarding_campos`: **catálogo declarativo (driver único)** — una fila por campo (entidad, campo=nombre exacto de columna, etiqueta, grupo, fuente, obligatorio, condicion, selector, inmutable, orden); alimenta formulario del cliente + panel por campo + completitud. (d) `cambios_propuestos`: **cola de validación GRANULAR por campo** (decisión Cristian) — lo que carga/corrige el cliente entra acá, no pisa lo oficial hasta aprobar (aplica) o devolver (con observación). (e) `import_kame_staging`: espejo crudo jsonb por `batch_id`. (f) `fn_rut_valido(text)` (módulo 11, NO security definer) y `fn_norm_texto(text)` (minúsculas+sin tildes+trim). (g) vistas **security_invoker** `v_onboarding_completitud` (computada, nunca stale: cruza catálogo vs `to_jsonb(registro)->>campo`, con regla condicional plan_isapre/AFP; expone `lleno`/`aplica`/`falta`) y `v_onboarding_resumen` (% por registro). Todo con RLS estándar (always-true equipo) + auditoría/updated_at donde aplica; `catalogo_valores`/`catalogo_variantes`/`onboarding_campos` con SELECT anon p/ el portal público (variantes NO expuesto a anon).
- **Pasada de limpieza ejecutada (29-06):** normalización vía variantes dejó **0 valores fuera de catálogo** en afp/salud/estado_civil/sexo/banco/tipo_cuenta/comuna; **split de apellidos** (paterno/materno desde `apellidos`, preservando el original) 131/132, queda 1 manual (compuesto/único); borrado el registro de prueba "Cristian Lopez 17.811.316-k" de Sargo. Completitud base: Alimentos C y RS 86%, … Distribuidora Barrera 38%, LeBlanc 37%.
- **Auditoría BD post-migración:** advisors sin ERROR (todo WARN); lo nuevo solo agrega `rls_policy_always_true` (mismo diseño del resto del panel). `fn_rut_valido`/`fn_norm_texto` no son SECURITY DEFINER. Índice de cobertura agregado a `cambios_propuestos.cliente_id`.
- **Paso 2 (✓ panel interno `/onboarding`, 29-06-2026):** ruta en sección Cartera del sidebar (registrada en `lib/modules.ts`). Vistas de apoyo `v_onboarding_empresas` (% ficha empresa + n° trabajadores y % promedio + faltantes) y `v_onboarding_por_campo` (requeridos/faltan por campo), ambas security_invoker. UI con 3 pestañas: **Empresas** (grilla ordenada por menor completitud, stat cards, selector inline de etapa de onboarding que estampa el hito, click → diálogo con campos faltantes de la ficha + de cada trabajador, agrupados y con badge de fuente), **Por campo** (ordenado por más faltantes para atacar en lotes, click → diálogo con los registros que no tienen ese dato), **Validación** (cola `cambios_propuestos`: aprobar aplica el valor a la tabla productiva con validación del campo contra `onboarding_campos`; devolver rechaza con observación — vacía hasta el Paso 3). Tipos/helpers en `lib/onboarding.ts`; page servidor + `onboarding-client.tsx` + `actions.ts` (drill-down lazy vía server actions, `setOnboardingEstado`, `aprobarCambio`, `devolverCambio`). Sigue el patrón de `/liquidaciones`. Verificado: `tsc --noEmit` limpio, sin errores de lint propios.
- **Pendiente:** **Paso 3** (flujo del cliente: onboarding cliente nuevo + completar pre-llenado en el portal, con checklist y gate progresivo, escribiendo a `cambios_propuestos` vía RPC SECURITY DEFINER por token). FK/CHECK duros contra selectores quedan diferidos hasta que la normalización cubra todo (hoy es soft). Para cargar TODAS las comunas del país (hoy V Región + RM + algunas) usar carga masiva a `catalogo_valores`.

## Pendientes operativos

- ⏳ **PRIORIDAD — Cargar `ANTHROPIC_API_KEY` en Vercel** (proyecto rstaxlegal-panel → Settings → Environment Variables → Production → Redeploy). Cristian tiene solicitud enviada para unirse a la org "RS Tax & Legal" en console.anthropic.com (alternativa: crear org propia + billing $5). **Esta key activa todo el pipeline de redacción IA ya cableado y desplegado**: (a) descripción de hechos de amonestaciones → párrafo formal en la carta; (b) "otras consideraciones remuneracionales" del cliente → cláusula adicional formal en el contrato. Sin la key, fallback al comportamiento manual (texto tal cual / solo cláusulas del equipo). Visión de Cristian: TODO texto libre del cliente pasa por IA para que los documentos salgan listos para revisar y enviar.
- ⏳ `RESEND_API_KEY` en Vercel (activa envío de documentos por correo con un click).

- Conseguir RUT y clave SII de Distribuidora Red Barrera e Inmobiliaria Barrera Álvarez.
- Asignar responsables F29 a los 123 ciclos (Solange o Danilo, definir con contador jefe).
- Marcar `es_profesional_salud=TRUE` en los clientes que correspondan.
- Migrar contraseñas SII y Previred a Bitwarden (Ley 21.719).

## Archivos de memoria por cliente

Política de la firma: al término de cada conversación o gestión relevante con un cliente, se actualiza el archivo de memoria `.md` de ese cliente, para que cualquier persona del equipo conozca el avance y gestiones de los demás. Cuando se trabaje un tema específico de un cliente, recordar al usuario actualizar el `.md` de ese cliente al cierre.

## Convenciones

- Todo en español: UI, nombres de funciones (`cargarDashboard`, `guardarCambios`, `abrirModal`), columnas de BD y comentarios.
- Fechas en UI: DD-MM-AAAA (la BD usa ISO `date`); montos en CLP con `toLocaleString('es-CL')`.
- Texto dinámico en tablas siempre pasa por `escapeHtml`.
- Dominio chileno: plazos Previred, F29 (SII), certificado KAME, cambios de IVA para profesionales de salud.
